# Example environment variables for integration test docker-compose
# Copy to .env or let CI workflow copy automatically.
# Safe test-only defaults (do not place secrets here).

REDIS_IMAGE=redis:7.2.4
REDIS_PASSWORD=redisMasterPass
