version: "2"
linters:
  default: none
  enable:
    - errcheck
    - goconst
    - gocritic
    - gocyclo
    - gosec
    - govet
    - ineffassign
    - staticcheck
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - errcheck
          - goconst
          - gocritic
          - gocyclo
          - gosec
          - govet
          - ineffassign
          - staticcheck
        path: _test\.go
      - linters:
          - gosec
        text: "G115"
        path: service/apple/pkcs7/
    paths:
      - third_party$
      - builtin$
      - examples$
  settings:
    gosec:
      excludes:
        - G117  # struct fields matching secret patterns (Password, AuthToken, Secret) – expected in auth APIs
    staticcheck:
      checks:
        - all        # enable all staticcheck checks
        - -ST1005    # disable capitalization rule for error strings
        - -ST1003    # disable rule for variable names
        - -ST1016    # disable receiver rules
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
      - _test\.go
